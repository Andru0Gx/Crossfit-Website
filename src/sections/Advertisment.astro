---
const input_class =
    "input border-2 border-black px-3 py-1 rounded-2xl focus:outline-none focus:border-primary transition-all duration-300 ease-in-out w-full";

// Agregar el 0 a los meses y dias menores a 10
const year = new Date().getFullYear();
const day = new Date().getDate().toString().padStart(2, "0");

const min =
    year +
    "-" +
    (new Date().getMonth() + 1).toString().padStart(2, "0") +
    "-" +
    day;
const max =
    year +
    "-" +
    (new Date().getMonth() + 2).toString().padStart(2, "0") +
    "-" +
    day;
---

<div
    class="bg-secondary w-fit h-fit py-5 px-8 rounded-2xl transition-transform duration-300 ease-in-out"
    id="inscribete"
>
    <h1 class="text-2xl text-center text-primary sm:text-4xl">
        ¡Primera Clase Gratis!
    </h1>
    <p class="text-center text-lg mt-5 text-wrap font-secondary">
        Ven y disfruta de una clase gratis en nuestro gimnasio, que esperas!
    </p>
    <p class="text-center text-sm text-gray-500 text-wrap font-secondary">
        (Cupon valido solo para clases presenciales)
    </p>
</div>

<style>
    .scale-105 {
        transform: scale(1.25);
    }

    .animate-scale {
        animation: scale 500ms infinite alternate;
    }

    @keyframes scale {
        0% {
            transform: scale(1);
        }
        100% {
            transform: scale(1.25);
        }
    }
</style>

<script>
    window.addEventListener("scroll", () => {
        const button = document.getElementById("inscribete");

        if (!button) return;

        if (window.scrollY > 200) {
            button.classList.add("scale-105");
        } else {
            button.classList.remove("scale-105");
        }
    });
</script>
